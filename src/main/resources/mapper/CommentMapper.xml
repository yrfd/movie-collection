<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.CommentMapper">

    <resultMap id="CommentMap" type="com.movie.entity.Comment">
        <id column="comment_id" property="commentId"/>
        <result column="movie_id" property="movieId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="reply_to" property="replyTo"/>
        <result column="is_edited" property="isEdited"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="snapshot_rating" property="snapshotRating"/>
        <result column="username" property="username"/>
        <result column="movie_name" property="movieName"/>
        <result column="current_rating" property="currentRating"/>
        <result column="reply_to_username" property="replyToUsername"/>
        <!-- 递归查询回复 -->
        <collection property="replies" ofType="com.movie.entity.Comment"
                    select="findRepliesByParentId" column="comment_id"/>
    </resultMap>

    <!-- 查询顶级评论（reply_to 为 NULL 或 0） -->
    <select id="findCommentsByMovieId" resultMap="CommentMap">
        SELECT c.*, u.username, m.movie_name, col.personal_rating as current_rating,
               ru.username as reply_to_username
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 JOIN movie_public m ON c.movie_id = m.movie_id
                 LEFT JOIN movie_collection col ON col.user_id = c.user_id AND col.movie_id = c.movie_id
                 LEFT JOIN user ru ON ru.user_id = c.reply_to
        WHERE c.movie_id = #{movieId}
          AND (c.reply_to IS NULL OR c.reply_to = 0)
        ORDER BY c.create_time DESC
    </select>

    <!-- 查询回复（使用 resultMap 以支持递归嵌套） -->
    <select id="findRepliesByParentId" resultMap="CommentMap">
        SELECT c.*,
               u.username,
               col.personal_rating as current_rating,
               ru.username as reply_to_username
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 LEFT JOIN movie_collection col ON col.user_id = c.user_id AND col.movie_id = c.movie_id
                 LEFT JOIN user ru ON ru.user_id = c.reply_to
        WHERE c.reply_to = #{comment_id}
        ORDER BY c.create_time ASC
    </select>

    <!-- 其他查询保持不变... -->
    <select id="findCommentById" resultMap="CommentMap">
        SELECT c.*, u.username, m.movie_name
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE c.comment_id = #{commentId}
    </select>

    <!-- 插入评论 - 合并方案：同时支持回复和自动获取评分 -->
    <insert id="insertComment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO movie_comment (movie_id, user_id, content, reply_to, snapshot_rating)
        SELECT #{movieId}, #{userId}, #{content}, #{replyTo}, col.personal_rating
        FROM movie_collection col
        WHERE col.user_id = #{userId} AND col.movie_id = #{movieId}
    </insert>

    <update id="updateComment">
        UPDATE movie_comment
        SET content = #{content},
            is_edited = TRUE,
            update_time = NOW()
        WHERE comment_id = #{commentId}
    </update>

    <delete id="deleteComment">
        DELETE FROM movie_comment WHERE comment_id = #{commentId}
    </delete>

    <select id="findCommentsByUserId" resultMap="CommentMap">
        SELECT c.*, u.username, m.movie_name
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE c.user_id = #{userId}
        ORDER BY c.create_time DESC
    </select>

    <!-- 评分相关查询 - 使用 snapshot_rating -->
    <select id="getAverageRating" resultType="java.lang.Double">
        SELECT AVG(snapshot_rating)
        FROM movie_comment
        WHERE movie_id = #{movieId}
          AND snapshot_rating IS NOT NULL
          AND snapshot_rating > 0
    </select>

    <select id="getRatingCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM movie_comment
        WHERE movie_id = #{movieId}
          AND snapshot_rating IS NOT NULL
          AND snapshot_rating > 0
    </select>

    <select id="findCommentsByTmdbId" resultMap="CommentMap">
        SELECT c.*, u.username, m.movie_name
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE m.tmdb_id = #{tmdbId}
        ORDER BY c.create_time DESC
    </select>

    <select id="getAverageRatingByTmdbId" resultType="java.lang.Double">
        SELECT AVG(c.snapshot_rating)
        FROM movie_comment c
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE m.tmdb_id = #{tmdbId}
          AND c.snapshot_rating IS NOT NULL
          AND c.snapshot_rating > 0
    </select>

    <select id="getRatingCountByTmdbId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM movie_comment c
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE m.tmdb_id = #{tmdbId}
          AND c.snapshot_rating IS NOT NULL
          AND c.snapshot_rating > 0
    </select>

    <select id="findCommentByUserAndMovie" resultMap="CommentMap">
        SELECT c.*, u.username, m.movie_name
        FROM movie_comment c
                 JOIN user u ON c.user_id = u.user_id
                 JOIN movie_public m ON c.movie_id = m.movie_id
        WHERE c.user_id = #{userId} AND c.movie_id = #{movieId}
            LIMIT 1
    </select>

    <update id="updateCommentRatingByUserAndMovie">
        UPDATE movie_comment
        SET snapshot_rating = #{newRating},
            update_time = NOW()
        WHERE user_id = #{userId} AND movie_id = #{movieId}
    </update>

</mapper>