<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.RatingMapper">

    <insert id="insertOrUpdateRating">
        INSERT INTO user_rating (user_id, movie_id, rating)
        VALUES (#{userId}, #{movieId}, #{rating})
            ON DUPLICATE KEY UPDATE
                                 rating = #{rating},
                                 update_time = NOW()
    </insert>

    <select id="getAverageRatingByMovie" resultType="java.lang.Double">
        SELECT AVG(rating)
        FROM user_rating
        WHERE movie_id = #{movieId}
    </select>

    <select id="getRatingCountByMovie" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user_rating
        WHERE movie_id = #{movieId}
    </select>

    <delete id="deleteRating">
        DELETE FROM user_rating
        WHERE user_id = #{userId} AND movie_id = #{movieId}
    </delete>

</mapper>